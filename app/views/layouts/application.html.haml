!!!
%html

  %head
    %title
      = yield(:title) || "Untitled"
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag "application", "formtastic", "formtastic_changes", :cache => "base"
    = stylesheet_link_tag "js_theme/overcast/jquery-ui-1.8.13.custom.css"
    = javascript_include_tag :defaults
    = csrf_meta_tag
    = yield(:head)

  %body
    #container
    
      %div#user_nav 
        -if user_signed_in?
          %span Signed in as
          = current_user.email
          %span Not you?
          = link_to "Sign out", destroy_user_session_path
        -else
          %p= link_to "Sign_up", new_user_registration_path
          %p= link_to "Sign_in", new_user_session_path
      
      %div#post_nav
        %h2= link_to "Post New Event", new_post_path
        %h2= link_to "Post New Category", new_category_path
    
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"

      - if show_title?
        %h1= yield(:title)

      = yield
